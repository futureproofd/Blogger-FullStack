const authController = require('./authController');
const blogController = require('./blogController');
const uploadController = require('./uploadController');

module.exports = { authController, blogController, uploadController };
